# Agent 工作规则

## 最高优先级

让普通用户在知情、授权和可回退的前提下完成个人代理搭建。任何“已经成功”的判断都要有可复查的测试结果。

## 开始前

1. 先读 `README.md`。
2. 再读 `.agents/skills/vps-chain-deployer/SKILL.md`。
3. 先根据用户需求选择直连、固定 SOCKS5/ISP 链式、双 VPS 或备用入口；服务端默认使用 VLESS + Reality。
4. 每次只向用户索取当前阶段所需的信息。

本仓库只保存通用产品流程。某一台 VPS 的历史、当前 IP、真实凭据、账单、模型实测和个人交接备注属于仓库外的本地上下文；没有拿到用户明确提供的本地上下文时，按新部署处理，不猜测现状。

## 人机分工

用户必须亲自完成：

- 注册、登录、付款、验证码、实名认证和服务条款确认。
- 决定预算、机房、套餐、续费方式、网络路径和是否接受重装风险。
- 确认自己有权管理目标服务器和代理账户。
- 批准会中断服务或改变安全状态的操作。
- 在个人设备上安装付费客户端、导入节点并检查联网结果。

Agent 可以完成：

- 比较用户提供的套餐页面，解释关键字段、成本和路由取舍。
- 生成 SSH 密钥，检查系统，部署和配置服务。
- 执行只读预检、连通性检查、日志诊断和交付整理。
- 指导 Shadowrocket、v2rayN 等客户端导入已生成的配置。

## 每次执行前要确认

- 重装系统、删除文件、覆盖配置。
- 修改防火墙、SSH 端口或登录方式。
- 安装第三方脚本或软件包。
- 重启 VPS、网络或代理服务。
- 打开设备的全局代理或路由全部流量。

确认内容要写清命令、影响范围、回退方式和预计中断时间。

## 凭据规则

- 不要求用户把支付密码、验证码、恢复码、SSH 私钥发进聊天。
- 不把 VPS 密码、完整 VLESS 链接、Reality 私钥、SOCKS5 密码写进仓库或执行记录。
- 不调用公共二维码网站处理节点链接；二维码仅在用户本机离线生成。
- 命令输出中出现密钥或完整链接时，交付记录只保留打码后的末四位或指纹。
- 怀疑凭据曾公开时，先轮换，再继续部署。

## 部署顺序

1. 核对所有权、预算、系统、IP、SSH 方式和用户选择的拓扑。
2. 创建回退点并完成服务器只读预检。
3. 部署单一 `VLESS + Reality` 入站。
4. 先验证 VPS 直连，再按用户确认接入固定 SOCKS5/ISP 或第二台 VPS。
5. 配置客户端，分别测试手机和电脑。
6. 按 IP、DNS、IPv6、WebRTC、延迟、丢包、速度和必要的 UDP 项验收。
7. 交付不含密钥的配置摘要、续费日期、恢复步骤和故障定位结果。

## 禁止范围

- 开放无认证代理、共享订阅服务或批量售卖节点。
- 扫描、入侵、撞库、流量攻击、恶意自动化和绕过平台风控。
- 未经授权操作第三方服务器、账号或网络。
- 为追求“能连上”而关闭全部防火墙、长期使用 root 密码或跳过来源审查。
- 未验证就宣称部署完成。

遇到越界需求时停止相关步骤，保留安全的诊断和恢复能力。
