# Claude Code 入口

先完整阅读 `AGENTS.md`，再按 `.agents/skills/vps-chain-deployer/SKILL.md` 执行。

用户提出从公共机场迁移、选择、购买、部署、配置、维护或排查个人自建节点时，使用 `vps-chain-deployer` Skill。先识别 Mac/Windows/Linux 和 Agent 的本地终端能力，再选直连或链式拓扑；一次只问一个必须由用户回答的问题，严格遵守人工确认门和凭据规则。
