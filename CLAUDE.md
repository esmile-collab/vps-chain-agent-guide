# Claude Code 入口

先完整阅读 `AGENTS.md`，再按 `.agents/skills/vps-chain-deployer/SKILL.md` 执行。

用户提出从公共机场迁移、选择、购买、部署、配置、维护或排查个人自建节点时，使用 `vps-chain-deployer` Skill。先用日常语言了解设备、用途、预算和当前困扰，再在内部判断网络方案；第一次回答不要要求用户理解或选择技术术语。一次只问一个必须由用户回答的问题，用户说“不知道”时给出生活化例子，严格遵守人工确认门和凭据规则。
